#ifndef __SETTING_2016_PASS__
#define __SETTING_2016_PASS__

#include <TF1.h>

const double Eb = 2.306;
const double CL_trk_time_Offset = 55.;
const double cl_t_max = 70.;
const double cl_t_min = 40.;
const double pos_d0_cut = 1.1;

//======= Tight cuts on trk-clust time diff =====
// These are dependent on track charge and also, they depend
// whether the track/cluster is in bot/top

const double mean_trk_clust_dt_ele_top = 0.05777;
const double sigm_trk_clust_dt_ele_top = 1.23;

const double mean_trk_clust_dt_ele_bot = -0.4263;
const double sigm_trk_clust_dt_ele_bot = 1.33;

const double mean_trk_clust_dt_pos_top = -8.61485e-02;
const double sigm_trk_clust_dt_pos_top = 1.2802;

const double mean_trk_clust_dt_pos_bot = -0.578381;
const double sigm_trk_clust_dt_pos_bot = 1.28636e+00;


// ===== Tight cuts on ele-pos cluster time difference =====
const double cl_dt_max = 0.45; // ns
const double cl_dt_min = -0.45; // ns

// ===== Tight cut on nsigma =====
// ===== Distributions look quite slimilar for ele/pos/bot/top
const double nsigma_tightcut = 3.;


// ===== Minimum electron momentum 'tight cut' =====
const double ele_min_mom = 0.4;  // GeV
const double ele_max_mom = 1.76; // GeV
const double tarP_Max = 2.9; // GeV
const double ele_chi2_tightcut = 2.5; 
const double pos_chi2_tightcut = 2.5; 


TF1 *f_mean_cl_trk_dt_ele_top = new TF1("f_mean_cl_trk_dt_ele_top", "-0.463849 + x*(1.01109) + x*x*(0.0489378) + x*x*x*(-0.258737)", 0., 2.5);
TF1 *f_sigm_cl_trk_dt_ele_top = new TF1("f_mean_cl_trk_dt_ele_top", "1.83648 + x*(-3.26601) + x*x*(6.07709) + x*x*x*(-4.88008) + x*x*x*x*(1.38874)", 0., 2.5);

TF1 *f_mean_cl_trk_dt_ele_bot = new TF1("f_mean_cl_trk_dt_ele_bot", "-0.812765 + x*(0.878871) + x*x*(-0.319924) + x*x*x*(0.00307882)", 0., 2.5);
TF1 *f_sigm_cl_trk_dt_ele_bot = new TF1("f_mean_cl_trk_dt_ele_bot", "1.34173 + x*(-0.704732) + x*x*(1.5466) + x*x*x*(-1.48625) + x*x*x*x*(0.486362)", 0., 2.5);

TF1 *f_mean_cl_trk_dt_pos_top = new TF1("f_mean_cl_trk_dt_pos_top", "-0.860302 + x*(2.1808) + x*x*(-1.62851) + x*x*x*(0.388061)", 0., 2.5);
TF1 *f_sigm_cl_trk_dt_pos_top = new TF1("f_mean_cl_trk_dt_pos_top", "2.79063 + x*(-7.3126) + x*x*(12.1243) + x*x*x*(-8.67505) + x*x*x*x*(2.23873)", 0., 2.5);

TF1 *f_mean_cl_trk_dt_pos_bot = new TF1("f_mean_cl_trk_dt_pos_bot", "-1.00867 + x*(2.89172) + x*x*(-2.12403) + x*x*x*(0.529724)", 0., 2.5);
TF1 *f_sigm_cl_trk_dt_pos_bot = new TF1("f_mean_cl_trk_dt_pos_bot", "0.067446 + x*(4.39018) + x*x*(-6.26695) + x*x*x*(3.86652) + x*x*x*x*(-0.881143)", 0., 2.5);

// P_max/P_min for trk-cluster matching functions, i.e. if the momentum is higher than this, then mean and sigma functions will be evaluated at these values
const double P_max_trk_cl_dt = 1.6;
const double P_min_trk_cl_dt = 0.4;


#endif
